export enum ProductType {
  BASE = 'BASE',
  ADDON = 'ADDON',
}

export enum DependencyRule {
  REQUIRED = 'REQUIRED',
  SUGGESTED = 'SUGGESTED',
}

export enum OrderStatus {
  PENDING = 'PENDING',
  CONFIRMED = 'CONFIRMED',
  SCOPED = 'SCOPED',
  CANCELLED = 'CANCELLED',
}